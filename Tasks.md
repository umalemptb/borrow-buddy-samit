# Borrow Buddy: Tasks

อ้างอิง [CONTEXT.md](./CONTEXT.md) และ [design.md](./design.md) ทำตามลำดับ ติ๊กเมื่อเสร็จ

กติกาการทำงาน: JavaScript เท่านั้น, ห้ามลบไฟล์โดยไม่ถามก่อน, ตอนตั้งค่า Vite/React/Vitest ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน

## เฟส 1: ตั้งโปรเจ็กต์

- [x] T1.1 สร้างโปรเจ็กต์ Vite + React (เทมเพลต JavaScript) ในโฟลเดอร์ `borrow-buddy` โดยไม่ทับ `CONTEXT.md` และ `design.md`
- [x] T1.2 ติดตั้ง Vitest และเพิ่มสคริปต์ `test` ใน `package.json`
- [x] T1.3 สร้างโครงโฟลเดอร์ `src/components` และ `src/lib` ตาม design.md ข้อ 7

- **เสร็จเมื่อ:** `npm run dev` เปิดหน้าเปล่าได้ และ `npm test` รันได้

## เฟส 2: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน

- [x] T2.1 `dateFormat.js`: แปลง `YYYY-MM-DD` เป็น พ.ศ. แบบ "24 ก.ย. 2569" + เทสต์
- [x] T2.2 `loanRules.js`: คำนวณสถานะ (ยังไม่คืน / เกินกำหนด / คืนแล้ว) รับ "วันนี้" เป็นพารามิเตอร์ + เทสต์ขอบ (วันนี้ = กำหนดคืนต้องยังไม่เกิน, วันถัดไปเกิน)
- [x] T2.3 `loanRules.js`: จำนวนวันเกินกำหนด + เทสต์
- [x] T2.4 `loanRules.js`: ตรวจความถูกต้องของ Loan (ชื่อเพื่อน/ของห้ามว่าง, กำหนดคืนและวันที่คืนจริงต้องไม่ก่อนวันที่ยืม) คืนข้อความผิดพลาดภาษาไทย + เทสต์ทุกกรณีผิด
- [x] T2.5 `loanRules.js`: จัดกลุ่ม เกินกำหนด → ยังไม่คืน → คืนแล้ว และเรียงตามกำหนดคืนใกล้สุดในแต่ละกลุ่ม + เทสต์
- [x] T2.6 `loanRules.js`: กรองตามชื่อเพื่อน + เทสต์
- [x] T2.7 `loanRules.js`: กดคืนแล้ว (ค่าเริ่มต้นวันนี้ แก้ย้อนหลังได้) และยกเลิกการคืน + เทสต์
- [x] T2.8 `storage.js`: อ่าน/เขียน Loan ใน localStorage คีย์เดียว, JSON เสียหรืออ่านไม่ได้ให้คืนรายการว่างพร้อมสัญญาณแจ้งเตือน ไม่เขียนทับข้อมูลเดิม

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 3: UI

- [x] T3.1 `App.jsx`: เก็บ state Loan โหลดจาก storage ตอนเปิด บันทึกทุกครั้งที่เปลี่ยน และแสดงข้อความภาษาไทยเมื่ออ่านข้อมูลไม่ได้
- [x] T3.2 `LoanForm.jsx`: ฟอร์มเดียวใช้เพิ่มและแก้ไข (เพื่อน, ของ, วันที่ยืม ค่าเริ่มต้นวันนี้, กำหนดคืน) แสดงข้อผิดพลาดจากการตรวจ มีปุ่มยกเลิกการแก้ไข
- [x] T3.3 `SearchBox.jsx`: ช่องค้นหาตามชื่อเพื่อน
- [x] T3.4 `LoanList.jsx` และ `LoanItem.jsx`: แสดงกลุ่มและรายการตาม design.md ข้อ 6 (วันที่ พ.ศ., จำนวนวันเกินเฉพาะเกินกำหนด, วันที่คืนจริงเฉพาะคืนแล้ว)
- [x] T3.5 ปุ่มต่อรายการ: คืนแล้ว (เลือกวันที่ได้), ยกเลิกการคืน, แก้ไข ไม่มีปุ่มลบ
- [x] T3.6 `ThemeToggle.jsx`: โหมดมืด สลับได้และจำค่าไว้
- [x] T3.7 สไตล์: รองรับมือถือ, สถานะแยกด้วยข้อความและสี (ไม่พึ่งสีอย่างเดียว)

- **เสร็จเมื่อ:** ใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้

## เฟส 4: ตรวจรับ

- [x] T4.1 รัน `npm test` ผ่านทั้งหมด
- [x] T4.2 ทดลองในเบราว์เซอร์: เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วข้อมูลยังอยู่
- [x] T4.3 ทดลองกรณีขอบ: กำหนดคืนวันนี้, กำหนดคืนเมื่อวาน, กรอกวันที่ผิดกติกา, ข้อมูลใน localStorage เสีย
- [x] T4.4 ตรวจหน้าจอมือถือและโหมดมืด
- [x] T4.5 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, สำรองข้อมูล) และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

## เฟส 5: เวอร์ชัน 2 (Supabase + เข้าสู่ระบบ + RLS)

อ้างอิง design.md เวอร์ชัน 2 ตอนใช้ supabase-js/Supabase Auth ให้ตรวจเอกสารล่าสุดผ่าน Context7 หรือ Supabase docs ก่อน ห้ามเพิ่มการลบ Loan

**5A: ฐานข้อมูลและความปลอดภัย**
- [x] T5.1 สร้าง migration ใน `supabase/migrations/`: ตาราง `loans` ตาม design.md ข้อ 4 (uuid, `owner_id` default `auth.uid()`, check ชื่อห้ามว่างและวันที่, index `owner_id`)
- [x] T5.2 ใน migration เดียวกัน: เปิด RLS, policy `select`/`insert`/`update` เฉพาะ `authenticated` และ `owner_id = (select auth.uid())`, ไม่มี policy `delete`, `revoke delete` จาก `authenticated`, `revoke all` จาก `anon`
- [x] T5.3 ตั้งค่า Supabase Auth: เปิด Email provider, ปิด "Allow new users to sign up", สร้างบัญชีเจ้าของด้วยมือใน Dashboard
- [x] T5.4 รัน security advisors ต้องไม่มีคำเตือนเกี่ยวกับ `loans`

**5B: ตั้งค่าโปรเจ็กต์**
- [x] T5.5 ติดตั้ง `@supabase/supabase-js`, สร้าง `.env.example` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`), ตรวจว่า `.env.local` อยู่ใน `.gitignore`
- [x] T5.6 `supabaseClient.js`: สร้าง client จาก env ที่เดียว แจ้งข้อความไทยชัดเจนถ้าไม่มี env

**5C: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน**
- [x] T5.7 `loanMapper.js`: แปลง row ↔ Loan + เทสต์ (ไม่ส่ง `id`/`owner_id` ตอนสร้าง)
- [x] T5.8 `loanRepo.js`: `listLoans`, `createLoan`, `updateLoan` รับ client เป็นพารามิเตอร์ คืนแถวจากฐานข้อมูลหรือข้อผิดพลาดภาษาไทย ไม่มีฟังก์ชันลบ + เทสต์ด้วย client จำลอง
- [x] T5.9 `authErrors.js`: แปลงข้อผิดพลาด Auth/เครือข่ายเป็นข้อความไทย (เข้าสู่ระบบผิดใช้ข้อความรวม ๆ) + เทสต์
- [x] T5.10 `legacyImport.js`: อ่านข้อมูลเดิมผ่าน `storage.js`, ตรวจด้วยกติกาเดิม, เตรียม row โดยไม่ส่ง `id` เดิม, จัดการธงนำเข้าแล้ว, ไม่ลบคีย์เดิม + เทสต์

**5D: UI**
- [x] T5.11 `App.jsx`: ติดตามเซสชัน (โหลดเซสชันเดิม + ฟังการเปลี่ยนแปลง) ยังไม่เข้าสู่ระบบแสดง `LoginForm` เข้าสู่ระบบแล้วโหลด Loan จาก Supabase แทน localStorage
- [x] T5.12 `LoginForm.jsx`: อีเมล + รหัสผ่าน, ปิดปุ่มระหว่างรอ, แสดงข้อผิดพลาดภาษาไทย ไม่มีลิงก์สมัครสมาชิก
- [x] T5.13 `AccountBar.jsx`: แสดงอีเมลและปุ่มออกจากระบบ (ล้าง Loan ออกจาก state)
- [x] T5.14 เปลี่ยนการเพิ่ม/แก้ไข/คืนแล้ว/ยกเลิกคืน ให้เรียก `loanRepo` ทีละรายการ อัปเดต state จากแถวที่ได้กลับ ถ้าล้มเหลวไม่เปลี่ยน state และแจ้งภาษาไทย
- [x] T5.15 สถานะกำลังโหลด, โหลดไม่สำเร็จพร้อมปุ่มลองใหม่, ปิดปุ่มระหว่างบันทึก, เซสชันหมดอายุกลับหน้าเข้าสู่ระบบพร้อมข้อความ
- [x] T5.16 `ImportBanner.jsx`: ถามนำเข้าข้อมูลเดิมเมื่อเข้าเงื่อนไข, นำเข้าแบบ batch, แจ้งจำนวนที่นำเข้า/ข้าม, ปฏิเสธได้
- [x] T5.17 เลิกเรียก `saveLoans` จาก UI (คงไฟล์ `storage.js` ไว้ ห้ามลบไฟล์)

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด และใช้งานครบทุกอย่างในขอบเขตเวอร์ชัน 2 ผ่านหน้าเว็บได้

**5E: ตรวจรับ**
- [x] T5.18 รัน `npm test` และ `npm run lint` ผ่านทั้งหมด
- [x] T5.19 ทดลองในเบราว์เซอร์: เข้าสู่ระบบ → เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วยังอยู่ → เปิดจากอีกเบราว์เซอร์แล้วเห็นข้อมูลเดียวกัน → ออกจากระบบแล้วไม่เห็น Loan (ยังไม่ได้ลองเปิดจากเบราว์เซอร์ที่สอง)
- [x] T5.20 ทดสอบ RLS ด้วยสองบัญชี: มองไม่เห็น/แก้ Loan ของอีกบัญชีไม่ได้, ไม่ได้เข้าสู่ระบบอ่านไม่ได้, ลบไม่ได้ทุกกรณี (ทดสอบด้วย SQL จำลองสองบัญชีแล้ว rollback, และเรียก REST แบบ anon ได้ 401)
- [x] T5.21 ยืนยันว่าสมัครสมาชิกผ่าน API ไม่ได้ และหน้าเว็บไม่มีฟอร์มสมัคร
- [x] T5.22 ทดลองนำเข้าข้อมูลเดิม: มีข้อมูลเดิม, มีรายการผิดกติกา, ปฏิเสธการนำเข้า, เปิดหน้าใหม่แล้วไม่ถามซ้ำ, ข้อมูลใน localStorage ยังอยู่ (ปุ่มไม่นำเข้าทดสอบด้วยเทสต์หน่วยเท่านั้น)
- [x] T5.23 ทดลองกรณีผิดพลาด: รหัสผ่านผิด, ตัดเน็ตระหว่างบันทึก, กรอกวันที่ผิดกติกา (รหัสผ่านผิดทดสอบด้วยเทสต์หน่วยเท่านั้น, ตัดเน็ตจำลองด้วยการแทน fetch)
- [x] T5.24 ตรวจว่าไม่มี secret/service_role key ในโค้ดหรือ git, ไม่มีฟีเจอร์นอกขอบเขต และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md
