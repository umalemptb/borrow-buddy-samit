# Borrow Buddy: เอกสารออกแบบ (เวอร์ชัน 2)

ศัพท์ทั้งหมดในเอกสารนี้อ้างอิงจาก [CONTEXT.md](./CONTEXT.md)

**สิ่งที่เปลี่ยนจากเวอร์ชันแรก**
- ย้ายที่เก็บ Loan จาก localStorage ไป Supabase (Postgres)
- เจ้าของต้อง **เข้าสู่ระบบ** ด้วยอีเมล + รหัสผ่าน และปิดการสมัครสมาชิกเอง
- ใช้ Row Level Security (RLS) ให้แต่ละบัญชีเห็นและแก้ได้เฉพาะ Loan ของตัวเอง
- **นำเข้าข้อมูลเดิม** จาก localStorage ได้หนึ่งครั้งหลังเข้าสู่ระบบ
- ยังไม่มีการลบ Loan เหมือนเดิม

## 1. ภาพรวมและเป้าหมาย
เว็บหน้าเดียวสำหรับ **เจ้าของ** ใช้บันทึกว่า **เพื่อน** ยืม **ของ** อะไร เมื่อไร ต้องคืนเมื่อไร และกดว่า **คืนแล้ว** ได้ ข้อมูลเก็บบนคลาวด์ จึงเปิดจากเครื่องไหนก็เห็นข้อมูลเดียวกันหลังเข้าสู่ระบบ

## 2. ขอบเขต

**อยู่ในขอบเขต**
- เข้าสู่ระบบด้วยอีเมล + รหัสผ่าน และออกจากระบบ
- เพิ่ม Loan, แก้ไข Loan, ดูรายการ Loan (เฉพาะของบัญชีตัวเอง)
- กดคืนแล้ว (ระบุวันที่คืนจริง) และยกเลิกการคืน
- ค้นหา/กรองตามชื่อเพื่อน
- แสดงสถานะ และจำนวนวันเกินกำหนด
- นำเข้าข้อมูลเดิมจาก localStorage หนึ่งครั้ง

**นอกขอบเขต (เวอร์ชัน 2)**
- สมัครสมาชิกเองผ่านหน้าเว็บ (บัญชีเจ้าของสร้างโดยผู้ดูแลใน Supabase Dashboard เท่านั้น)
- ลืมรหัสผ่าน/รีเซ็ตรหัสผ่านผ่านหน้าเว็บ, ล็อกอินด้วยโซเชียลหรือ magic link
- ลบ Loan, แจ้งเตือน, เลื่อนกำหนดคืน, รูปภาพของ
- ทะเบียนรายชื่อเพื่อน/ของแยก, การตรวจของซ้ำซ้อน
- ใช้งานแบบออฟไลน์, การซิงก์แบบเรียลไทม์ระหว่างแท็บ
- สำรองข้อมูล/นำเข้า-ส่งออกไฟล์ (นอกเหนือจากการนำเข้าข้อมูลเดิมครั้งเดียว)

## 3. เทคโนโลยี
- React + Vite, ภาษา JavaScript (ไม่ใช้ TypeScript)
- Supabase: Postgres สำหรับเก็บ Loan, Supabase Auth (อีเมล + รหัสผ่าน), RLS
- ไลบรารี `@supabase/supabase-js` ฝั่งเบราว์เซอร์
- ค่าเชื่อมต่อผ่าน environment variable ของ Vite: `VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY` เก็บใน `.env.local` (ไม่ commit)
- ห้ามใช้ `service_role` / secret key ในโค้ดฝั่งเบราว์เซอร์เด็ดขาด
- ทดสอบด้วย Vitest
- การตั้งค่าโหมดมืดยังเก็บใน localStorage (เป็นค่าของเครื่อง ไม่ใช่ข้อมูล Loan)

## 4. โมเดลข้อมูล

**ฝั่งแอป (JavaScript)**: รูปแบบเดิม เพิ่มเพียงการที่ `id` มาจากฐานข้อมูล

| ฟิลด์ | ชนิด | หมายเหตุ |
|---|---|---|
| `id` | string (uuid) | ฐานข้อมูลสร้างให้ |
| `friendName` | string | ชื่อเล่นของเพื่อน พิมพ์เองทุกครั้ง ห้ามว่าง |
| `itemName` | string | ชื่อของ ห้ามว่าง |
| `borrowedDate` | string `YYYY-MM-DD` | วันที่ยืม |
| `dueDate` | string `YYYY-MM-DD` | กำหนดคืน บังคับกรอก |
| `returnedDate` | string `YYYY-MM-DD` หรือ `null` | วันที่คืนจริง ว่างจนกว่าจะกดคืน |

**ฝั่งฐานข้อมูล**: ตาราง `public.loans`

| คอลัมน์ | ชนิด | ข้อกำหนด |
|---|---|---|
| `id` | `uuid` | primary key, default `gen_random_uuid()` |
| `owner_id` | `uuid` | not null, default `auth.uid()`, references `auth.users(id)` |
| `friend_name` | `text` | not null, `check (btrim(friend_name) <> '')` |
| `item_name` | `text` | not null, `check (btrim(item_name) <> '')` |
| `borrowed_date` | `date` | not null |
| `due_date` | `date` | not null, `check (due_date >= borrowed_date)` |
| `returned_date` | `date` | null ได้, `check (returned_date is null or returned_date >= borrowed_date)` |
| `created_at` | `timestamptz` | not null, default `now()` |

- มี index ที่ `owner_id`
- แปลงชื่อฟิลด์ camelCase ↔ snake_case ใน `lib/` ที่เดียว ส่วนอื่นของแอปใช้รูปแบบฝั่งแอปเท่านั้น
- ข้อกำหนด `check` ในฐานข้อมูลเป็นด่านสุดท้าย แอปยังต้องตรวจเองก่อนส่งและแสดงข้อความภาษาไทยเหมือนเดิม
- วันที่เก็บเป็น ค.ศ. และแสดงผลเป็น พ.ศ. เท่านั้น (เช่น 24 ก.ย. 2569) สถานะและจำนวนวันเกินกำหนดไม่เก็บ คำนวณจากวันที่ทุกครั้ง

## 5. กติกา

**สถานะ** (คำนวณจากวันนี้)
- **คืนแล้ว**: `returnedDate` ไม่เป็น null
- **เกินกำหนด**: ยังไม่คืน และวันนี้ > `dueDate`
- **ยังไม่คืน**: ยังไม่คืน และวันนี้ ≤ `dueDate` (ถึงกำหนดวันนี้พอดียังไม่เกิน)

**จำนวนวันเกินกำหนด** = วันนี้ − `dueDate` (นับเป็นวัน ใช้กับ Loan ที่เกินกำหนดเท่านั้น)

**การตรวจวันที่** (ปฏิเสธการบันทึกเมื่อผิดกติกา พร้อมข้อความภาษาไทย)
- `dueDate` ต้องไม่ก่อน `borrowedDate`
- `returnedDate` ต้องไม่ก่อน `borrowedDate`
- ชื่อเพื่อนและชื่อของห้ามว่าง

**การกดคืนแล้ว**
- ค่าเริ่มต้นของวันที่คืนจริงคือวันนี้ แก้เป็นวันย้อนหลังได้
- ยกเลิกการคืนได้ (ตั้ง `returnedDate` กลับเป็น null)

**การเข้าสู่ระบบ**
- ใช้อีเมล + รหัสผ่าน ผ่าน Supabase Auth
- ยังไม่เข้าสู่ระบบ: เห็นเฉพาะฟอร์มเข้าสู่ระบบ ไม่โหลดและไม่แสดง Loan
- เข้าสู่ระบบไม่สำเร็จ: แสดงข้อความภาษาไทยแบบรวม ๆ ("อีเมลหรือรหัสผ่านไม่ถูกต้อง") ไม่บอกว่าผิดช่องไหน
- เซสชันถูกจำไว้ (ตามค่าเริ่มต้นของ supabase-js) เปิดหน้าใหม่ไม่ต้องเข้าสู่ระบบซ้ำจนกว่าจะออกจากระบบหรือเซสชันหมดอายุ
- ออกจากระบบ: ล้าง Loan ออกจาก state ทันที
- เซสชันหมดอายุระหว่างใช้งาน: กลับไปหน้าเข้าสู่ระบบพร้อมข้อความภาษาไทย

**การนำเข้าข้อมูลเดิม**
- หลังเข้าสู่ระบบ ถ้าพบ Loan ในคีย์ localStorage เดิม (`borrow-buddy:loans`) และเครื่องนี้ยังไม่เคยนำเข้า ให้แสดงแถบถามว่าจะนำเข้าหรือไม่ (ไม่นำเข้าอัตโนมัติ)
- นำเข้า: ตรวจทุกรายการด้วยกติกาเดิม ส่งรายการที่ถูกต้องขึ้น Supabase ครั้งเดียวแบบ batch โดยไม่ส่ง `id` เดิม (ให้ฐานข้อมูลสร้างใหม่) แล้วแจ้งจำนวนที่นำเข้าได้/ข้ามไป
- ไม่ลบข้อมูลใน localStorage เดิม บันทึกแค่ธงว่าเครื่องนี้นำเข้าแล้ว เพื่อไม่ถามซ้ำและไม่นำเข้าซ้ำ
- ปฏิเสธการนำเข้าได้ (บันทึกธงเช่นกัน) และข้อมูลเดิมยังอยู่

## 6. ความปลอดภัยและ RLS

**Auth**
- ปิด "Allow new users to sign up" ใน Supabase Auth ทำให้ `signUp` จากหน้าเว็บใช้ไม่ได้
- สร้างบัญชีเจ้าของด้วยมือใน Supabase Dashboard (Authentication → Users → Add user) พร้อมยืนยันอีเมล
- หน้าเว็บไม่มีปุ่มหรือฟอร์มสมัครสมาชิก

**RLS บนตาราง `loans`**
- เปิด RLS (`enable row level security`)
- `select`: เฉพาะ role `authenticated` และ `owner_id = (select auth.uid())`
- `insert`: เฉพาะ `authenticated` และ `with check (owner_id = (select auth.uid()))`
- `update`: เฉพาะ `authenticated`, `using` และ `with check` เป็น `owner_id = (select auth.uid())` (ย้าย Loan ไปเป็นของคนอื่นไม่ได้)
- `delete`: **ไม่มี policy** จึงลบไม่ได้ และ `revoke delete` จาก `authenticated` ซ้ำอีกชั้น
- role `anon` ไม่มีสิทธิ์ใด ๆ บนตาราง (`revoke all ... from anon`)
- แอปไม่ต้องส่ง `owner_id` เอง ปล่อยให้ default `auth.uid()` ใส่ให้

**การยืนยัน**
- ตรวจด้วย Supabase advisors (security) ต้องไม่มีคำเตือนเรื่อง RLS ของตาราง `loans`
- ทดสอบด้วยสองบัญชี: บัญชี A มองไม่เห็นและแก้ Loan ของบัญชี B ไม่ได้, ไม่ได้เข้าสู่ระบบอ่านไม่ได้เลย, ลบไม่ได้ทุกกรณี

## 7. หน้าจอ (หน้าเดียว)

**ยังไม่เข้าสู่ระบบ**
- **ฟอร์มเข้าสู่ระบบ**: อีเมล, รหัสผ่าน, ปุ่มเข้าสู่ระบบ (ปิดปุ่มระหว่างรอ) และปุ่มสลับโหมดมืด

**เข้าสู่ระบบแล้ว**
- **แถบบน**: อีเมลของเจ้าของ, ปุ่ม **ออกจากระบบ**, ปุ่มสลับโหมดมืด
- **แถบนำเข้าข้อมูลเดิม** (เฉพาะเมื่อเข้าเงื่อนไขข้อ 5)
- **ฟอร์ม Loan** ด้านบน ใช้ทั้งเพิ่มและแก้ไข: เพื่อน, ของ, วันที่ยืม (ค่าเริ่มต้นวันนี้), กำหนดคืน กดแก้ไขที่รายการแล้วข้อมูลขึ้นฟอร์ม มีปุ่มยกเลิกการแก้ไข
- **ช่องค้นหา/กรอง** ตามชื่อเพื่อน
- **รายการ Loan** ด้านล่าง แยกกลุ่มตามลำดับ **เกินกำหนด → ยังไม่คืน → คืนแล้ว** แต่ละกลุ่มเรียงตามกำหนดคืนใกล้สุดก่อน
- แต่ละรายการแสดง: ชื่อของ, ชื่อเพื่อน, วันที่ยืม, กำหนดคืน, สถานะ, จำนวนวันเกินกำหนด (เฉพาะเกินกำหนด), วันที่คืนจริง (เฉพาะคืนแล้ว)
- ปุ่มต่อรายการ: **คืนแล้ว** (เฉพาะที่ยังไม่คืน), **ยกเลิกการคืน** (เฉพาะที่คืนแล้ว), **แก้ไข**
- ไม่มีปุ่มลบ

**สถานะการโหลด/บันทึก**
- ระหว่างโหลดรายการครั้งแรก แสดง "กำลังโหลด..."
- ระหว่างบันทึก ปิดปุ่มที่เกี่ยวข้องชั่วคราวเพื่อกันกดซ้ำ

**การแสดงผล**
- ภาษาไทยทั้งหมด, วันที่ พ.ศ.
- รองรับหน้าจอมือถือ (layout ยืดหยุ่น)
- โหมดมืด สลับได้ด้วยปุ่ม และจำค่าที่เลือกไว้
- ใช้สี/ข้อความแยกสถานะให้ชัด ไม่พึ่งสีอย่างเดียว

## 8. โครงสร้างโค้ด (ระดับโฟลเดอร์)

แยกตรรกะล้วนออกจาก UI และแยกการเรียก Supabase ไว้ในชั้นเดียว เพื่อให้ทดสอบด้วย client จำลองได้

```
borrow-buddy/
├── CONTEXT.md
├── design.md
├── .env.example             # ตัวอย่างชื่อตัวแปร ไม่มีค่าจริง
├── package.json
├── index.html
├── supabase/
│   └── migrations/          # SQL สร้างตาราง loans, check, index, RLS, revoke
└── src/
    ├── main.jsx
    ├── App.jsx              # ติดตามเซสชัน เลือกแสดงหน้าเข้าสู่ระบบหรือหน้ารายการ
    ├── components/
    │   ├── LoginForm.jsx    # ใหม่
    │   ├── AccountBar.jsx   # ใหม่: อีเมล + ออกจากระบบ
    │   ├── ImportBanner.jsx # ใหม่: นำเข้าข้อมูลเดิม
    │   ├── LoanPage.jsx     # ใหม่: state/โหลด/บันทึก Loan ของบัญชีที่เข้าสู่ระบบ (App ใส่ key เป็น id บัญชี)
    │   ├── LoanForm.jsx
    │   ├── LoanList.jsx
    │   ├── LoanItem.jsx
    │   ├── SearchBox.jsx
    │   └── ThemeToggle.jsx
    └── lib/
        ├── loanRules.js      # ไม่เปลี่ยน
        ├── dateFormat.js     # ไม่เปลี่ยน
        ├── theme.js          # ไม่เปลี่ยน
        ├── supabaseClient.js # ใหม่: สร้าง client จาก env ที่เดียว
        ├── loanMapper.js     # ใหม่: แปลง row ↔ Loan (ฟังก์ชันล้วน)
        ├── loanRepo.js       # ใหม่: list/create/update Loan รับ client เป็นพารามิเตอร์ ไม่มี delete
        ├── authErrors.js     # ใหม่: แปลงข้อผิดพลาด Auth/เครือข่ายเป็นข้อความไทย
        ├── legacyImport.js   # ใหม่: อ่าน Loan เดิม, ตรวจ, เตรียม row, จัดการธงนำเข้าแล้ว
        └── storage.js        # เดิม: เหลือไว้ให้ legacyImport อ่านอย่างเดียว ไม่ใช้บันทึก Loan อีก
```

- ฟังก์ชันใน `lib/` รับ "วันนี้" และ client/storage เป็นพารามิเตอร์ ไม่เรียกเวลาจริงหรือ client จริงข้างใน เพื่อให้ทดสอบได้แน่นอน
- `storage.js` ไม่ลบทิ้ง แต่ `saveLoans` เลิกใช้จาก UI

## 9. การเก็บข้อมูล
- Loan อยู่ในตาราง `loans` ของ Supabase เท่านั้น
- โหลดรายการทั้งหมดของบัญชีตอนเข้าสู่ระบบสำเร็จ (RLS กรองให้ ไม่ต้องใส่เงื่อนไข owner ในคิวรี)
- บันทึกทีละรายการ: เพิ่ม = `insert` หนึ่งแถว, แก้ไข/คืนแล้ว/ยกเลิกคืน = `update` แถวนั้น (ไม่เขียนทั้งอาร์เรย์เหมือนเวอร์ชันแรก)
- อัปเดต state จากแถวที่ฐานข้อมูลส่งกลับ (`select()` หลังเขียน) ไม่เดาผลล่วงหน้า
- บันทึกไม่สำเร็จ (เครือข่าย, RLS, check): ไม่เปลี่ยน state, คงข้อมูลในฟอร์มไว้ และแจ้งเป็นภาษาไทย
- โหลดไม่สำเร็จ: แสดงข้อความภาษาไทยพร้อมปุ่มลองใหม่ ห้ามแสดงเป็นรายการว่างเหมือนไม่มีข้อมูล

## 10. การทดสอบ

**Vitest (ตรรกะใน `lib/`)**
- ของเดิมทั้งหมด: สถานะ, ขอบเขตวันกำหนดคืน, จำนวนวันเกิน, การตรวจวันที่, จัดกลุ่ม/เรียง, แปลง พ.ศ., กรองชื่อเพื่อน
- `loanMapper`: แปลงไป-กลับครบทุกฟิลด์, `returnedDate` null, ไม่ส่ง `id`/`owner_id` ตอนสร้าง
- `loanRepo`: ใช้ client จำลอง ตรวจว่าเรียกตาราง/เมธอดถูก, คืนข้อผิดพลาดภาษาไทยเมื่อ client error, ไม่มีฟังก์ชันลบ
- `authErrors`: แปลงข้อผิดพลาดที่พบบ่อยเป็นข้อความไทย
- `legacyImport`: ไม่มีข้อมูลเดิม, JSON เสีย, รายการผิดกติกาถูกข้าม, ไม่ส่ง `id` เดิม, ธงนำเข้าแล้วทำให้ไม่ถามซ้ำ, ไม่ลบคีย์เดิม

**ตรวจด้วยมือ/SQL**
- RLS ด้วยสองบัญชีตามข้อ 6
- การสมัครสมาชิกจากหน้าเว็บ/API ถูกปฏิเสธ
- Supabase security advisors ไม่มีคำเตือนที่เกี่ยวกับ `loans`

## 11. ประเด็นที่ตั้งใจไม่ทำ
สมัครสมาชิกเอง, รีเซ็ตรหัสผ่านผ่านเว็บ, ออฟไลน์/เรียลไทม์, สำรองข้อมูล, ลบ Loan, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, ทะเบียนเพื่อน/ของ หากต้องการในอนาคตให้ทบทวน CONTEXT.md ก่อน
